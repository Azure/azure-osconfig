name: Check performance
description: Checks the performance log for failures

inputs:
  perflog:
    description: the performance log path
    required: true
  mark:
    description: the performance failure marker
    required: true

runs:
  using: composite
  steps:
    - name: Check for performance failures
      env:
        perflog: ${{ inputs.perflog }}
        mark: ${{ inputs.mark }}
      run: |
        failed=false

        echo "Checking '$perflog' for any performance warnings ('$mark')"
        ls -l "$perflog"
        echo "Contents of '$perflog':"
        cat "$perflog"        

        failures=$(grep -i "$mark" $perflog || true)

        if [ -n "$failures" ]; then
            echo "Found performance failures in '$perflog'"
            echo "Failures:"
            echo "$failures"
            failed=true
        else
            echo "Found no performance warnings in '$perflog'"
        fi

        if [ "$failed" == true ]
        then
            echo "Check performance test failed"
            exit 1
        fi
      shell: bash
